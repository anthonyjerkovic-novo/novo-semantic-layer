cubes:
  - name: credit_card_requests
    sql_table: "DEV_DB.DEV_ANWAR.FACT_CREDIT_CARD_REQUESTS"
    public: false

    joins:
      - name: businesses
        sql: "{CUBE}.business_id = {businesses}.business_id"
        relationship: one_to_one

    dimensions:
      - name: business_id
        sql: "{CUBE}.business_id"
        type: string
        primary_key: true
        title: Business ID
        description: Unique identifier for the business

      - name: credit_card_requested_at
        sql: "{CUBE}.credit_card_requested_at"
        type: time
        title: Credit Card Requested At
        description: Earliest timestamp when a credit card request was created for a business.

    measures:
      - name: credit_card_attach
        sql: "{CUBE}.business_id"
        type: count_distinct
        title: Credit Card Attach (#)
        description: Count of distinct businesses that requested a credit card in a given time period.

    pre_aggregations:
      # Pre-aggregation definitions can be added here for performance optimization
      # Learn more: https://cube.dev/docs/caching/pre-aggregations/getting-started
