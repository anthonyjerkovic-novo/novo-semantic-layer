views:
  - name: vw_monthly_balances
    
    cubes:

      - join_path: monthly_balances
        prefix: false
        includes:
          # dimensions
          - balance_month
          # measures
          - month_end_balance
          - avg_daily_balance_aggr_monthly

      - join_path: monthly_balances.businesses
        prefix: false
        includes:
          # Dimensions
          - opened_account_cohort
          - business_type
          - business_industry_category
          - b2b_services_flag
          - age_of_business
          - tenure
