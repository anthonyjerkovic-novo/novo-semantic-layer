views:
  - name: vw_express_ach_revenue
    
    cubes:

      - join_path: express_ach_revenue
        prefix: false
        includes:
          # dimensions
          - transaction_at
          # measures
          - express_ach_revenue

      - join_path: express_ach_revenue.businesses
        prefix: false
        includes:
          # Dimensions
          - opened_account_cohort
          - business_type
          - business_industry_category
          - b2b_services_flag
          - age_of_business
          - tenure
