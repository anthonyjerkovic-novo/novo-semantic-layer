views:
  - name: vw_domestic_wire_revenue
    
    cubes:

      - join_path: domestic_wire_revenue
        prefix: false
        includes:
          # dimensions
          - wire_created_at
          # measures
          - domestic_wire_revenue

      - join_path: domestic_wire_revenue.businesses
        prefix: false
        includes:
          # Dimensions
          - opened_account_cohort
          - business_type
          - business_industry_category
          - b2b_services_flag
          - age_of_business
          - tenure
