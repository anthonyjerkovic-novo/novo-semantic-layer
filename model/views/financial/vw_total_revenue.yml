views:
  - name: vw_total_revenue
    
    cubes:

      - join_path: total_revenue
        prefix: false
        includes:
          # dimensions
          - reporting_month
          # measures
          - revenue_excluding_interchange
          - expenses
          - marginal_net_income

      - join_path: total_revenue.businesses
        prefix: false
        includes:
          # Dimensions
          - opened_account_cohort
          - business_type
          - business_industry_category
          - b2b_services_flag
          - age_of_business
          - tenure
