views:
  - name: vw_funding_activity
    
    cubes:

      - join_path: funding_activity
        prefix: false
        includes:
          # dimensions
          - funding_transaction_at
          # measures
          - mca_attach
          - mca_active

      - join_path: funding_activity.businesses
        prefix: false
        includes:
          # Dimensions
          - opened_account_cohort
          - business_type
          - business_industry_category
          - b2b_services_flag
          - age_of_business
          - tenure
